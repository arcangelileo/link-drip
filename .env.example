# ---- Application ----
# Display name shown in the UI
APP_NAME=LinkDrip

# Public-facing URL (used to generate short links and QR codes)
APP_URL=http://localhost:8000

# Secret key for signing JWT tokens — MUST be changed in production
# Generate one with: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=change-me-to-a-random-secret-key

# Set to false in production
DEBUG=true

# ---- Database ----
# SQLite (default) — for production, point to a persistent path
DATABASE_URL=sqlite+aiosqlite:///./linkdrip.db

# ---- JWT Authentication ----
# Signing algorithm (HS256 recommended)
JWT_ALGORITHM=HS256

# Token lifetime in minutes (default: 1440 = 24 hours)
JWT_EXPIRATION_MINUTES=1440
